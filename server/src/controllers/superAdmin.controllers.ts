import { NextFunction, Request, Response } from "express";
import { asyncHandler } from "../utils/asyncHandler.util.js";




export const loginSuperAdmin = asyncHandler(async (req: Request, res: Response, next: NextFunction) => {
    
})

export const logoutSuperAdmin = asyncHandler(async (req: Request, res: Response, next: NextFunction) => {
    
})

